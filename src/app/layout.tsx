import type { Metadata } from "next";
import { Halant } from "next/font/google";
import { Inter } from "next/font/google";
import "./globals.css";
import "@/lib/gsap-setup";
import { ServiceWrapper } from "@/components/ServiceWrapper";
import Tag from "@/tag/Tag";
import { getVisualEditScript } from "@/utils/visual-edit-script";
import { Montserrat } from "next/font/google";



export const metadata: Metadata = {
  title: 'Ollywood Food Park - Best Local Restaurant in Khordha',
  description: 'Visit Ollywood Food Park in Khordha for authentic flavors, polite service, and a great atmosphere. Order online or visit us today!',
  keywords: ["restaurant Khordha, food park, Ollywood food, best food near Khordha, dine-in delivery"],
  openGraph: {
    "title": "Ollywood Food Park",
    "description": "Experience authentic flavors at Ollywood Food Park.",
    "siteName": "Ollywood Food Park",
    "type": "website"
  },
};

const montserrat = Montserrat({
  variable: "--font-montserrat",
  subsets: ["latin"],
});
const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <ServiceWrapper>
        <body className={`${montserrat.variable} ${inter.variable} antialiased`}>
          <Tag />
          {children}
          <script
              dangerouslySetInnerHTML={{
                  __html: `${getVisualEditScript()}`
              }}
          />
        </body>
      </ServiceWrapper>
    </html>
  );
}
